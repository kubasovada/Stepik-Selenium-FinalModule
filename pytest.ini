[pytest]
markers = smoke: marker for smoke tests
          regression: marker for regression tests
          need_review
          need_review_custom_scenarios
          login_guest
          test_user
